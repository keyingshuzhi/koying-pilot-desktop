/**
 * Koying Pilot About surface, node half. The browser implementation is
 * discovered through the package's `dsh.client` declaration.
 */

/** Host plugin body — product attribution is browser-only. */
export function apply(): void {}
